# Comparator

## What it is

A Verilog comparator module.

## Why it matters

It is a foundational combinational block used throughout control and datapath logic.

## How it's built

`comparator.v` contains the RTL.

## How to run

Compile `comparator.v` in a Verilog simulator and supply test vectors.
